# 闪现 (sx) Phase 4 — 回归测试记录

| 项 | 内容 |
|----|------|
| 文档版本 | v1.0 |
| 测试类型 | 功能 RC / 内测基线回归预备 |
| 测试阶段 | Phase 4 |
| 关联 Task | Epic F (F-01) |
| 验证状态 | **编译构建与代码走查 PASS，等待实机/模拟器点按实测** |

---

## 一、 回归路径规划与走查列表

根据 `docs/开发指令_Phase4.md` 四、回归最低路径，拟定以下 5 项关键测试路径及代码逻辑走查结果：

1. **激活授权流程**：
   - 走查逻辑：`SplashActivity` / `LicenseActivity` 校验 `SX-DEV-20991231` HMAC 签名。
   - 代码状态：逻辑已通，构建正常。待实机点按。
2. **沙箱多开管理**：
   - 走查逻辑：`AppPickerActivity` 选择 App -> `BlackBoxSandboxEngine.install` -> 启动/克隆多 userId -> 清数据与卸载。
   - 代码状态：逻辑已通，BlackBox 依赖编译正确。待实机点按。
3. **环境伪装探针校验**：
   - 走查逻辑：`SpoofProbeActivity` 检查 Location/Device/WiFi/Cell 注入读数。
   - 代码状态：探针 Activity 与 Hook 代理代码均已接入。待实机点按。
4. **相机预览伪装验证**：
   - 走查逻辑：`VirtualCameraActivity` 配置替代图片源，`PreviewCallback` 注入。
   - 代码状态：`VirtualCameraManager` 策略实现完成。待实机点按。
5. **未激活拦截机制**：
   - 走查逻辑：`LicenseManager.isActivated()` 双向拦截沙箱 launch 与 UI 操作。
   - 代码状态：已加入 Toast 与拦截。待实机点按。

---

## 二、 模拟器与真机待测试用例表 (待实测)

| 序号 | 路径 / 功能点 | 验证要点 | 代码走查/构建状态 | 实机测试状态 |
|-----|--------------|----------|-------------------|--------------|
| 1.1 | 首次启动与激活 | 输入 `SX-DEV-20991231` 激活跳转三 Tab | 已通过 Gradle 构建验证 | 待实测 |
| 1.2 | 三 Tab 切换 | 首页、应用、我的无崩溃 | 已通过 ViewBinding 检查 | 待实测 |
| 1.3 | 沙箱应用导入 | 已安装 App 导入引擎 | BlackBox API 接入完成 | 待实测 |
| 1.4 | 启动 / 克隆 / 卸载 | 数据隔离、多实例运行 | `BlackBoxSandboxEngine` 编译无错 | 待实测 |
| 1.5 | 伪装探针验证 | 检查 Location/Device/WiFi 读数 | Hook 代理代码已覆盖 | 待实测 |
| 1.6 | 相机预览替换 | PreviewCallback 帧替换 | Manager 代码已集成 | 待实测 |
| 1.7 | 无授权拦截 | 未激活禁止启动分身 | `LicenseGate` 双向拦截已加 | 待实测 |

---

## 三、 结论与后续说明

- **编译构建验证**：`assembleDebug` + `assembleRelease` (含 R8 混淆) **100% 编译构建通过**。
- **功能逻辑走查**：Phase 0-4 核心代码路径均已接入并完成语法与结构验证。
- **实机运行回归**：由于 Agent 环境限制，GUI 界面交互测试标定为 **待实测 (Pending Device Test)**，需部署至真机/模拟器后由人工或 ADB 自动化脚本完成最终点按验收。
