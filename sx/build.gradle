// Top-level build file for 闪现 (sx)
plugins {
    id 'com.android.application' version '8.2.2' apply false
}

ext {
    compileSdkVersion = 30
    buildToolsVersion = "30.0.3"
    targetSdkVersion = 30
    blackReflection = '1.1.2'
}
