<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- release：仅允许 HTTPS，禁止明文 HTTP -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
